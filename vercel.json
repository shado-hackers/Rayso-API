{
    "version": 2,
    "builds": [
        {
            "src": "*.json",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "vercel.json"
        }
    ]
}
